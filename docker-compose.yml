services:
  neo4j:
    image: neo4j:5.18
    container_name: yu_neo4j
    ports:
      - "7474:7474"   # Browser UI
      - "7687:7687"   # Bolt
    environment:
      - NEO4J_AUTH=neo4j/yu_chatbot_2026
    volumes:
      - neo4j_data:/data
    restart: unless-stopped

volumes:
  neo4j_data:
